Temporary trigger file for CI measurement runs on this fork.

The screenshot workflow ignores .github/** and **.md paths, so a PR that only
edits the workflow file would never trigger it. This file exists to make the
PR contain at least one non-ignored path. Safe to delete; never intended for
upstream.
